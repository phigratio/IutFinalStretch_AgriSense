# Optional: force the backend URL. When unset, the app auto-uses the Metro
# host (the laptop running `npx expo start`) on port 3000 — correct whenever
# backend + Expo run on the same laptop and the phone shares its network.
# EXPO_PUBLIC_API_URL=http://192.168.0.100:3000
