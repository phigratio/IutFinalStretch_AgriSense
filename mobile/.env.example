# Optional: force the backend URL. When unset, the app uses the HOSTED backend
# (see deploy/README.md); on Expo Go with a real LAN Metro host it uses that
# laptop on port 3000. Set this ONLY for local backend development.
# EXPO_PUBLIC_API_URL=http://192.168.0.100:3000
